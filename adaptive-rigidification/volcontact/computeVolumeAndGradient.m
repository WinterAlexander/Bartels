function [V, dVdp] = computeVolumeAndGradient( meshes )
% computeVolumeAndGradient Computes intersection volume and gradient by
% raytracing the meshes on given intervals in the x and y axis.

nx = 128;
rangex = [-3, 3];
ny = 128;
rangey = [-4, 2];

[bufferx, countx] = 

end